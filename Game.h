/*#pragma once
#include <vector>
class Game
{
public:

	void move_up(std::vector <int> maze, int player_position);
	void move_left(std::vector <int> maze, int player_position);
	void move_down(std::vector <int> maze, int player_position);
	void move_right(std::vector <int> maze, int player_position);

	void move(char a, std::vector <int> maze, int player_position) {

		switch (a) {

		case 'w': move_up(maze, player_position); break;
		case 'a': move_left(maze, player_position); break;
		case 's': move_down(maze, player_position); break;
		case 'd': move_right(maze, player_position); break;

		}
			
		return;

	}

	void print_maze(std::vector <int> maze) {

		//for (size_t i = 0; i < )
	
	
	}

};
*/
